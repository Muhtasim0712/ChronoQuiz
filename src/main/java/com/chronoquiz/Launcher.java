package com.chronoquiz;

/**
 * Standard Java main entry point.
 * This class does NOT extend javafx.application.Application, allowing the application
 * to run seamlessly in IDEs (like IntelliJ IDEA) and fat JARs without requiring
 * special '--module-path' VM options.
 */
public class Launcher {
    public static void main(String[] args) {
        Main.main(args);
    }
}
